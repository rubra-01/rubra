# render.yaml

services:
  - type: web
    name: rubra-website
    env: static
    buildCommand: ""
    staticPublishPath: "."
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
    envVars:
      - key: NODE_VERSION
        value: 18.x
